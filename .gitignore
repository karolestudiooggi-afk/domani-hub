# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets & local config — NUNCA commitar (use os .example como modelo)
.mcp.json
.claude/settings.local.json
.env
.env.*
!.env.example

# Auditoria de segurança (anon key + JWT + dados exfiltrados) — NUNCA commitar
audit/

# Testes (artefatos)
/coverage/
/test-results/
/playwright-report/
/playwright/.cache/

# --- Auditorias movidas p/ bilhonos/auditorias/ — NUNCA commitar artefatos de audit (contêm segredos) ---
audit/
.re-audit/
audit-artifacts/
