/**
 * Bilhon Light Violeta: efeito de fundo animado removido do tema.
 * Mantemos o export como no-op para preservar imports existentes sem quebrar builds.
 */
export function AnimatedBackground() {
  return null;
}
